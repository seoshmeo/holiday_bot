# Обновление бота до версии 2.0

## На вашем сервере выполните:

```bash
# 1. Остановите бота
sudo systemctl stop holiday-traditions-bot

# 2. Перейдите в папку бота
cd ~/bots/holiday_bot

# 3. Обновите код
git pull

# 4. Активируйте виртуальное окружение
source venv/bin/activate

# 5. Обновите зависимости (добавлен requests)
pip install -r requirements.txt

# 6. Запустите бота
sudo systemctl start holiday-traditions-bot

# 7. Проверьте статус
sudo systemctl status holiday-traditions-bot
```

## Что изменилось?

### Новые файлы:
- `database.py` - модуль для работы с SQLite
- `images.py` - интеграция с Unsplash
- `holiday_bot.db` - база данных (создастся автоматически)
- `CHANGELOG.md` - история изменений

### Обновлённые файлы:
- `bot.py` - новый функционал
- `requirements.txt` - добавлен requests
- `config.py` - обновлён системный промпт
- `.gitignore` - исключены файлы базы данных

### Новые команды бота:
- `/custom` - выбор страны вручную
- `/stats` - статистика кэша

## Проверка работы

Откройте бота в Telegram и попробуйте:

1. `/stats` - должна показаться статистика (пока 0)
2. `/custom` - новый интерактивный выбор
3. `/holiday` - теперь с изображениями!

## База данных

База данных создастся автоматически при первом запуске.

Расположение: `~/bots/holiday_bot/holiday_bot.db`

Для просмотра:
```bash
sqlite3 ~/bots/holiday_bot/holiday_bot.db
.tables
.schema
SELECT * FROM holiday_responses;
.quit
```

## Логи

Проверьте логи после обновления:

```bash
# Логи в реальном времени
sudo journalctl -u holiday-traditions-bot -f

# Последние 50 строк
sudo journalctl -u holiday-traditions-bot -n 50
```

## Откат (если что-то пошло не так)

```bash
cd ~/bots/holiday_bot
git checkout a90161e  # Предыдущая версия
pip install -r requirements.txt
sudo systemctl restart holiday-traditions-bot
```

## Проблемы?

### База данных не создаётся
- Проверьте права доступа к папке
- Убедитесь, что бот запущен от правильного пользователя

### Изображения не отправляются
- Это нормально, бот работает и без них
- Проверьте доступ к Unsplash через curl
- Посмотрите логи на наличие ошибок

### Команда /custom не работает
- Перезапустите бота
- Проверьте, что обновился файл bot.py

## Преимущества версии 2.0

✅ **Кэширование** - повторные запросы мгновенны
✅ **Экономия токенов** - одинаковые ответы берутся из кэша
✅ **Изображения** - визуально привлекательнее
✅ **Гибкость** - пользователь сам выбирает страну
✅ **Статистика** - видно, сколько стран в кэше

---

**Готово! Бот обновлён до версии 2.0** 🎉
