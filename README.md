# Holiday Traditions Bot

Telegram-бот, который генерирует информацию о том, как празднуют Рождество и Новый год в разных странах мира.

## Возможности

- Генерация праздничных традиций для 50+ стран
- Информация о традиционных нарядах, обычаях, блюдах
- Рекомендации фильмов и музыки по теме
- Советы по адаптации традиций дома
- Интерактивные кнопки для быстрой навигации

## Структура проекта

```
holiday_bot/
│
├── bot.py              # Основная логика Telegram бота
├── llm.py              # Интеграция с OpenAI API
├── countries.py        # Список стран и типов праздников
├── config.py           # Конфигурация и настройки
├── requirements.txt    # Зависимости Python
├── .env.example        # Пример файла с переменными окружения
└── README.md           # Документация
```

## Установка

### 1. Клонирование и установка зависимостей

```bash
cd holiday_bot
python -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate
pip install -r requirements.txt
```

### 2. Получение токенов

#### Telegram Bot Token

1. Откройте [@BotFather](https://t.me/botfather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям и выберите имя бота
4. Скопируйте полученный токен

#### OpenAI API Key

1. Зарегистрируйтесь на [platform.openai.com](https://platform.openai.com)
2. Перейдите в раздел API Keys
3. Создайте новый ключ и скопируйте его

### 3. Настройка переменных окружения

Создайте файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
```

Отредактируйте `.env` и добавьте свои токены:

```env
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL=gpt-4o-mini
```

## Запуск

### Тестирование LLM модуля

Перед запуском бота можно протестировать подключение к OpenAI:

```bash
python llm.py
```

Вы должны увидеть успешное подключение и пример генерации традиций для Финляндии.

### Запуск бота

```bash
python bot.py
```

После запуска вы увидите сообщение:

```
Бот запускается...
```

Теперь бот готов к работе!

## Команды бота

- `/start` — Приветствие и описание функций
- `/holiday` — Случайная страна и случайный праздник
- `/christmas` — Традиции Рождества в случайной стране
- `/newyear` — Традиции Нового года в случайной стране
- `/another` — Повторить генерацию с другой страной

## Пример использования

1. Откройте вашего бота в Telegram
2. Отправьте `/start` для приветствия
3. Отправьте `/holiday` для получения случайной традиции
4. Нажмите кнопку "Другая страна" для генерации новой традиции

## Пример вывода

```
🎉 Страна: Финляндия
Праздник: Рождество

👗 Наряд: шерстяной свитер с узором оленей
🍽 Традиции: утренняя сауна, рождественская каша с миндалем, визит Йоулупукки
🎬 Фильм: "Rare Exports: A Christmas Tale"
🎶 Музыка: "Joulupuu on rakennettu"
💡 Совет: устрой домашнюю сауну и подари друзьям маленькие свечи — как в Финляндии.
```

## Технические детали

### Используемые технологии

- **Python 3.8+**
- **python-telegram-bot** — библиотека для работы с Telegram Bot API
- **OpenAI API** — генерация контента через GPT модели
- **python-dotenv** — управление переменными окружения

### Модель OpenAI

По умолчанию используется `gpt-4o-mini` как оптимальный баланс между качеством и стоимостью. Вы можете изменить модель в файле `.env`:

```env
OPENAI_MODEL=gpt-4o  # Для более качественных ответов
```

### Системный промпт

Системный промпт для LLM находится в [config.py:28-46](config.py#L28-L46). Он определяет формат и стиль ответов бота.

## Дополнительные возможности

### Добавление новых стран

Отредактируйте файл [countries.py](countries.py) и добавьте страны в список `COUNTRIES`:

```python
COUNTRIES = [
    # ... существующие страны
    "Новая страна",
]
```

### Изменение формата вывода

Измените системный промпт в [config.py:28-46](config.py#L28-L46) для изменения структуры ответа.

## Устранение неполадок

### Ошибка: "TELEGRAM_BOT_TOKEN не найден"

Убедитесь, что:
1. Файл `.env` создан
2. В нем указан корректный токен от BotFather
3. Нет лишних пробелов вокруг значения

### Ошибка: "OPENAI_API_KEY не найден"

Убедитесь, что:
1. У вас есть активный API ключ от OpenAI
2. На вашем аккаунте OpenAI есть доступные средства
3. Ключ корректно скопирован в `.env`

### Ошибка при генерации текста

Проверьте:
1. Баланс на аккаунте OpenAI
2. Доступность модели (некоторые модели требуют специального доступа)
3. Лимиты API (rate limits)

### Бот не отвечает

1. Проверьте, запущен ли бот (`python bot.py`)
2. Проверьте логи в консоли
3. Убедитесь, что токен бота корректный
4. Попробуйте перезапустить бота

## Идеи для расширения

- Добавить поддержку нескольких языков
- Реализовать ежедневную рассылку "Праздник дня"
- Добавить quiz-режим "Угадай страну по традиции"
- Интегрировать изображения через DALL-E или Unsplash
- Добавить систему избранного
- Реализовать поиск по конкретной стране
- Добавить статистику популярных запросов

## Лицензия

MIT License

## Автор

Создано с помощью Claude Code
